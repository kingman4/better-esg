# Server
PORT=8080

# Database (individual components — used if DATABASE_URL is not set)
DB_HOST=postgres
DB_PORT=5432
DB_USER=esg
DB_PASSWORD=esg
DB_NAME=esg
DB_SSLMODE=disable

# Or provide a full connection string instead:
# DATABASE_URL=postgres://esg:esg@postgres:5432/esg?sslmode=disable

# FDA ESG NextGen API (not yet wired up)
# FDA_CLIENT_ID=
# FDA_CLIENT_SECRET=
